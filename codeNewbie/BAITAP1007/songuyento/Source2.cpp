#include <stdio.h>
int main()
{
	int i=2, n, D;
	while (i <= n / 2)
	{
		if (n % i == 0)
		{	D++;
	}
		if (D == 0)
		{
			printf("%d la so nguyen to", n);
		}
		else {
			printf(" %d khong la so nuyen to", n);
		}


	}






}