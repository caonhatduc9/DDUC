#include <iostream>
using namespace std;
int main()
{
	int A[100];
	int n,min;
	printf("nhap so luong:");
	scanf_s("%d", &n);
	for (int i = 0; i < n; i++)
	{
		printf("nhap A[%d]", i);
		scanf_s("%d", &A[i]);
	}
	for (int i = 0; i < n; i++)
	{
		if(A[i]>0)
		{
			if (A[i])  
		    
		}
	}
	if (s == 0)
		printf("-1");
	return 0;
}